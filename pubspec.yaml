name: flutterando_link
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  auto_injector: ^2.0.5
  uno: ^1.1.12
  result_dart: ^1.1.1
  asp: ^2.0.3
  dson_adapter: ^1.2.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/body.json

